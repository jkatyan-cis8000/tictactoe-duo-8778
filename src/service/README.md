# Service Layer

Business logic for the Tic-Tac-Toe game.

Contains:
- apply_move(): compute new game state after a move
- is_valid_move(): check if move is legal
- format_board(): render board as string
- format_game(): render full game state as string
